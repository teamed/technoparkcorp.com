<!-- this script is called by breadcrumbs() helper -->

<? $first = true; ?>

<? foreach ($this->pages as $page): ?>

    <? if (!$first): ?>
        &#32;/&#32;
    <? else: ?>
        <? $first = false; ?>
    <? endif; ?>

    <? $id = 'header_' . md5($page->title); ?>

    <span id="<?=$id?>" class="page">
        <a href="<?=$this->panelUrl($page->title)?>"
            title="go to <?=$this->escape($page->title)?>"><?=$this->escape($page->label)?></a><? if ($page->parent->count() > 1): ?><span
            onclick="selector($('#<?=$id?>'), '<?=$page->title?>')"
            title="click to change">&darr;</span>
        <? endif; ?>
    </span>

<? endforeach; ?>

<? $this->includeJS('header.js'); ?>