<h1>Access restricted</h1>

<p class="flash">
    <? if ($this->message): ?>
        <?=$this->message?>
    <? else: ?>
        Unknown reason
    <? endif; ?>
</p>

<p>This particular document is not available for you. Here is
a list of documents that you can access:</p>

<p><?=$this->navigation()
    ->menu(Model_Pages::getInstance())
    ->setMaxDepth(1)?></p>