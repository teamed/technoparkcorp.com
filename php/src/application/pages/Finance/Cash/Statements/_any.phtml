<? $statement = $this->root->statements[$this->segment()] ?>

<h1>Statement for <?=$statement->supplier?></h1>

<p>Balance to pay: <b><?=$statement->balance?></b>,
    volume: <?=$statement->volume?> 
    (<?=count($statement)?> payments).</p>

<?=$this->table()
    ->setSource($statement)
    ->addColumn('created', 'Date')
    ->addColumn('usd', 'Amount')
    ->addColumn('rate', 'Rate')
    ->addColumn('context', 'Context')
    ->addColumn('reason', 'Reason')
    ->addColumn('details', 'Details')
    ?>
