
<? foreach ($this->root->projectRegistry as $name=>$project): ?>

    [<?=$name?>]
    <? foreach ($project->staffAssignments->getEverybody() as $email): ?>
        <?=$email?> = rw
    <? endforeach; ?>

<? endforeach; ?>
