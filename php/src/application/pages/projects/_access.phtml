
<? foreach ($this->root->projectRegistry as $name=>$project): ?>

    [<?=$name?>]
    <? foreach ($project->staffAssignments as $stakeholder): ?>
        <?=$stakeholder?> = rw
    <? endforeach; ?>

<? endforeach; ?>
