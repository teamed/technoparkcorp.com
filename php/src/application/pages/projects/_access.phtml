
<? foreach ($this->root->projectRegistry as $name=>$project): ?>

    [<?=$name?>]
    <? foreach ($project->getStakeholders() as $email=>$pwd): ?>
        <?=$email?> = rw
    <? endforeach; ?>

<? endforeach; ?>
