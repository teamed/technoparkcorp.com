<h1>Staff Assignments</h1>

<?=$this->table()
    ->setSource($this->project->staffAssignments)
    ->addColumn('email', 'Stakeholder/Email')
    ->addColumn('rolesString', 'Roles assigned')
    ?>
    
<p>Project access configuration file (<tt>authz</tt>) is used:</p>
    
<pre><?=$this->project->fzProject()->authz?></pre>